@model hmsAPI2.Models.Diagnosis

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Diagnosis</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="ICD10_code" />
            <div class="form-group">
                <label asp-for="DiagnosisName" class="control-label"></label>
                <input asp-for="DiagnosisName" class="form-control" />
                <span asp-validation-for="DiagnosisName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="DateDiagnosed" class="control-label"></label>
                <input asp-for="DateDiagnosed" class="form-control" />
                <span asp-validation-for="DateDiagnosed" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
