@model IEnumerable<hmsAPI2.Models.ContraIndication>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ActiveEngredients)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Diagnosis)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ActiveEngredients)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Diagnosis)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ICD10_code">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ICD10_code">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ICD10_code">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
